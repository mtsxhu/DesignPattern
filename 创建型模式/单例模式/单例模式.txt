
定义：保证一个类只有一个实例存在，同时提供能对该实例加以访问的全局访问方法。

饿汉式单例：在程序运行时就创建实例，多线程安全
懒汉式单例：在需要的时候才创建实例，多线程不安全
多线程安全的懒汉式单例：懒汉式单例由于在获取实例时需要判断实例是否已经创建，因此不适用于多线程场景

方法：
    1、将构造函数设为私有
    2、对外提供一个静态的获取实例的方法（设为静态方法是为了可以通过类名访问，而不需要通过实例）


GO语言实现单例是通过结构体名和全局变量名小写，来限制其他包访问结构体以及全局变量，只能通过
对外提供的函数，来获取全局唯一实例